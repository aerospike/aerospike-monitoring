[PARSER]
    # Aerospike RegEx patterns - 1 
    Name aero_log_parser_type_1
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+fds: proto \((?<ProtoConnNow>\d+),(?<ProtoConnOpen>\d+),(?<ProtoConnClose>\d+)\) heartbeat \((?<HBConnNow>\d+),(?<HBConnOpen>\d+),(?<HBConnClose>\d+)\) fabric \((?<FabricConnNow>\d+),(?<FabricConnOpen>\d+),(?<FabricConnClose>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 2 
    Name aero_log_parser_type_2
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} index-flash-usage: used-bytes (?<IdxFlashUseByte>\d+) used-pct (?<IdxFlashUsePct>\d+) alloc-bytes (?<IdxFlashAlBt>\d+) alloc-pct (?<IdxFlashAlPct>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 3 
    Name aero_log_parser_type_3
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+system-memory: free-kbytes (?<FreeMemKB>\d+) free-pct (?<FreeMemPct>\d+) heap-kbytes \((?<HeapKBAlloc>\d+),(?<HeapKBActive>\d+),(?<HeapKBMapped>\d+)\) heap-efficiency-pct (?<HeapEfficPct>[0-9\\.]+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 4 
    Name aero_log_parser_type_4
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} nsup-start: expire-threads (?<nsupExpireThrds>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 5 
    Name aero_log_parser_type_5
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} xdr-dc (?<xdr5dc>[^:]+): lag (?<xdr5lag>\d+) throughput (?<xdr5throughput>\d+) in-queue (?<xdr5inqueue>\d+) in-progress (?<xdr5outstanding>\d+) complete \((?<xdr5DoneOk>\d+),(?<xdr5DoneAbandon>\d+),(?<xdr5DoneNotFnd>\d+),(?<xdr5DoneFOut>\d+)\) retries \((?<xdr5RetrNoNode>\d+),(?<xdr5RetrConnRst>\d+),(?<xdr5RetrDest>\d+)\) recoveries \((?<xdr5Recov>\d+),(?<xdr5RecovPend>\d+)\) hot-keys (?<xdr5HotKeys>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 6 
    Name aero_log_parser_type_6
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} device-usage: used-bytes (?<DevUsedBytes>\d+) avail-pct (?<DevAvailPct>[0-9\\.]+) cache-read-pct (?<DevCacheReadPct>[0-9\\.]+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 7 
    Name aero_log_parser_type_7
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+batch-index: batches \((?<BatchSuccess>\d+),(?<BatchError>\d+),(?<BatchTimeout>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 8 
    Name aero_log_parser_type_8
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} rebalanced: regime (?<reRegime>\d+) expected-migrations \((?<reTx>\d+),(?<reRx>\d+),(?<reSig>\d+)\) expected-appeals (?<reAppeals>\d+) unavailable-partitions (?<reUnavail>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 9 
    Name aero_log_parser_type_9
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} query: basic \((?<QueryBasicOk>\d+),(?<QueryBasicErr>\d+),(?<QueryBasicAbrt>\d+)\) aggr \((?<QueryAggrOk>\d+),(?<QueryAggrErr>\d+),(?<QueryAggrAbrt>\d+)\) udf-bg \((?<QueryUdfOk>\d+),(?<QueryUdfErr>\d+),(?<QueryUdfAbrt>\d+)\) ops-bg \((?<QueryOpsOk>\d+),(?<QueryOpsErr>\d+),(?<QueryOpsAbrt>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 10 
    Name aero_log_parser_type_10
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+dlog: free-pct (?<xdrDlgFreePct>[0-9\\.]+) reclaimed (?<xdrDlogReclaim>\d+) glst (?<xdrDlogGlst>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 11 
    Name aero_log_parser_type_11
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} rebalanced: expected-migrations \((?<reTx>\d+),(?<reRx>\d+),(?<reSig>\d+)\) fresh-partitions (?<reFresh>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 12 
    Name aero_log_parser_type_12
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} re-repl: all-triggers \((?<ReReplAllTrgOk>\d+),(?<ReReplAllTrgErr>\d+),(?<ReReplAllTrgTou>\d+)\) unreplicated-records (?<ReReplUn>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 13 
    Name aero_log_parser_type_13
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} memory-usage: total-bytes (?<NsMemTotalBytes>\d+) index-bytes (?<NsMemIndexBytes>\d+) set-index-bytes (?<NsMemSetIdxByte>\d+) sindex-bytes (?<NsMemSindexByte>\d+) data-bytes (?<NsMemDataByte>\d+) used-pct (?<NsMemUsedPct>[0-9\\.]+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 14 
    Name aero_log_parser_type_14
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} scan: basic \((?<ScanBasicOk>\d+),(?<ScanBasicErr>\d+),(?<ScanBasicAbrt>\d+)\) aggr \((?<ScanAggrOk>\d+),(?<ScanAggrErr>\d+),(?<ScanAggrAbrt>\d+)\) udf-bg \((?<ScanUdfOk>\d+),(?<ScanUdfErr>\d+),(?<ScanUdfAbrt>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 15 
    Name aero_log_parser_type_15
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} nsup-done: evict-threads (?<nsupEvictThrds>\d+) expire-threads (?<nsupExpireThrds>\d+) non-expirable (?<nsupNonExpirabl>\d+) expired \((?<nsupExpireTotal>\d+),(?<nsupExpireNow>\d+)\) evicted \((?<nsupEvictTotal>\d+),(?<nsupEvictNow>\d+)\) evict-ttl (?<nsupEvictTtl>[0-9\\-]+) total-ms (?<nsupTotalMs>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 16 
    Name aero_log_parser_type_16
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} client: tsvc \((?<ClientTsvcErr>\d+),(?<ClientTsvcTout>\d+)\) proxy \((?<ClientPrxyOk>\d+),(?<ClientPrxyErr>\d+),(?<ClientPrxyTout>\d+)\) read \((?<ClientReadOk>\d+),(?<ClientReadErr>\d+),(?<ClientReadTout>\d+),(?<ClientReadNFnd>\d+),(?<ClientReadFltr>\d+)\) write \((?<ClientWrtOk>\d+),(?<ClientWrtErr>\d+),(?<ClientWrtTout>\d+),(?<ClientWrtFltr>\d+)\) delete \((?<ClientDelOk>\d+),(?<ClientDelErr>\d+),(?<ClientDelTout>\d+),(?<ClientDelNFnd>\d+),(?<ClientDelFltr>\d+)\) udf \((?<ClientUdfOk>\d+),(?<ClientUdfErr>\d+),(?<ClientUdfTout>\d+),(?<ClientUdfFltr>\d+)\) lang \((?<ClientLangRead>\d+),(?<ClientLangWrite>\d+),(?<ClientLangDel>\d+),(?<ClientlangErr>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 17 
    Name aero_log_parser_type_17
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} from-proxy: tsvc \((?<FrPrxyTsvcErr>\d+),(?<FrPrxyTsvcTout>\d+)\) read \((?<FrPrxyReadOk>\d+),(?<FrPrxyReadErr>\d+),(?<FrPrxyReadTout>\d+),(?<FrPrxyReadNFnd>\d+)\) write \((?<FrPrxyWrtOk>\d+),(?<FrPrxyWrtErr>\d+),(?<FrPrxyWrtTout>\d+)\) delete \((?<FrPrxyDelOk>\d+),(?<FrPrxyDelErr>\d+),(?<FrPrxyDelTout>\d+),(?<FrPrxyDelNFnd>\d+)\) udf \((?<FrPrxyUdfOk>\d+),(?<FrPrxyUdfErr>\d+),(?<FrPrxyUdfTout>\d+)\) lang \((?<FrPrxyLangRead>\d+),(?<FrPrxyLangWrite>\d+),(?<FrPrxyLangDel>\d+),(?<FrPrxylangErr>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 18 
    Name aero_log_parser_type_18
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} memory-usage: total-bytes (?<NsMemTotalBytes>\d+) index-bytes (?<NsMemIndexBytes>\d+) set-index-bytes (?<NsMemSetIdxByte>\d+) sindex-bytes (?<NsMemSindexByte>\d+) used-pct (?<NsMemUsedPct>[0-9\\.]+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 19 
    Name aero_log_parser_type_19
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} (?<Device>[^:]+): used-bytes (?<UsedBytes>\d+) free-wblocks (?<FreeWBlocks>\d+) write-q (?<WriteQ>\d+) write \((?<Writes>\d+),(?<WritePerSec>[0-9\\.]+)\) defrag-q (?<DefragQ>\d+) defrag-read \((?<DefragR>\d+),(?<DefragRPerSec>[0-9\\.]+)\) defrag-write \((?<DefragW>\d+),(?<DefragWPerSec>[0-9\\.]+)\) tomb-raider-read \((?<TombDevRd>\d+),(?<TombDevRdSec>[0-9\\.]+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 20 
    Name aero_log_parser_type_20
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+sub:  1 build $
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 21 
    Name aero_log_parser_type_21
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+xdr-dc (?<xdr5dc>[^:]+): lag (?<xdr5lag>\d+) throughput (?<xdr5throughput>\d+) latency-ms (?<xdr5latencyms>\d+) in-queue (?<xdr5inqueue>\d+) outstanding (?<xdr5outstanding>\d+) complete \((?<xdr5DoneOk>\d+),(?<xdr5DoneAbandon>\d+),(?<xdr5DoneNotFnd>\d+),(?<xdr5DoneFOut>\d+)\) retries \((?<xdr5RetrConnRst>\d+),(?<xdr5RetrDest>\d+)\) recoveries \((?<xdr5Recov>\d+),(?<xdr5RecovPend>\d+)\) hot-keys (?<xdr5HotKeys>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 22 
    Name aero_log_parser_type_22
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} index-flash-usage: used-bytes (?<IdxFlashUseByte>\d+) used-pct (?<IdxFlashUsePct>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 23 
    Name aero_log_parser_type_23
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} rebalanced: regime (?<reRegime>\d+) expected-migrations \((?<reTx>\d+),(?<reRx>\d+),(?<reSig>\d+)\) expected-appeals (?<reAppeals>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 24 
    Name aero_log_parser_type_24
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+regex:  quiesced this node$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 25 
    Name aero_log_parser_type_25
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} (?<Device>[^:]+): used-bytes (?<UsedBytes>\d+) free-wblocks (?<FreeWBlocks>\d+) write \((?<Writes>\d+),(?<WritePerSec>[0-9\\.]+)\) defrag-q (?<DefragQ>\d+) defrag-read \((?<DefragR>\d+),(?<DefragRPerSec>[0-9\\.]+)\) defrag-write \((?<DefragW>\d+),(?<DefragWPerSec>[0-9\\.]+)\) write-q (?<WriteQ>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 26 
    Name aero_log_parser_type_26
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} retransmits: migration (?<ResmitMigra>\d+) client-read (?<ResmitRead>\d+) client-write \((?<ResmitWrtDup>\d+),(?<ResmitWrtRepl>\d+)\) client-delete \((?<ResmitDelDup>\d+),(?<ResmitDelRepl>\d+)\) client-udf \((?<ResmitUdfDup>\d+),(?<ResmitRepl>\d+)\) batch-sub (?<ResmitBtchDup>\d+) udf-sub \((?<ResmitUdfDupSub>\d+),(?<ResmitUdfRepl>\d+)\) nsup (?<ResmitNsup>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 27 
    Name aero_log_parser_type_27
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} memory-usage: total-bytes (?<NsMemTotalBytes>\d+) index-bytes (?<NsMemIndexBytes>\d+) sindex-bytes (?<NsMemSindexByte>\d+) used-pct (?<NsMemUsedPct>[0-9\\.]+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 28 
    Name aero_log_parser_type_28
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} (?<Device>[^:]+): used-bytes (?<UsedBytes>\d+) free-wblocks (?<FreeWBlocks>\d+) write-q (?<WriteQ>\d+) write \((?<Writes>\d+),(?<WritePerSec>[0-9\\.]+)\) defrag-q (?<DefragQ>\d+) defrag-read \((?<DefragR>\d+),(?<DefragRPerSec>[0-9\\.]+)\) defrag-write \((?<DefragW>\d+),(?<DefragWPerSec>[0-9\\.]+)\) shadow-write-q (?<ShadowWriteQ>\d+) tomb-raider-read \((?<TombDevRd>\d+),(?<TombDevRdSec>[0-9\\.]+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 29 
    Name aero_log_parser_type_29
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+quiesced this (?<thisQuiesce>\d+) node$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 30 
    Name aero_log_parser_type_30
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} (?<Device>[^:]+): used-bytes (?<UsedBytes>\d+) free-wblocks (?<FreeWBlocks>\d+) write-q (?<WriteQ>\d+) write \((?<Writes>\d+),(?<WritePerSec>[0-9\\.]+)\) defrag-q (?<DefragQ>\d+) defrag-read \((?<DefragR>\d+),(?<DefragRPerSec>[0-9\\.]+)\) defrag-write \((?<DefragW>\d+),(?<DefragWPerSec>[0-9\\.]+)\) shadow-write-q (?<ShadowWriteQ>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 31 
    Name aero_log_parser_type_31
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+[A-Z][a-z]{2} [0-9]{2} [0-9]{4} [0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{3}$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 32 
    Name aero_log_parser_type_32
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+\\[(?<XdrDcName>[^]]+)\\]: dc-state (?<XdrDcState>[^ ]+) timelag-sec (?<XdrDcTimelag>\d+) lst (?<XdrDcLst>\d+) mlst (?<XdrDcMlst>\d+) \([^)]+\) fnlst (?<XdrDcFnlst>\d+) \([^)]+\) wslst (?<XdrDcWslst>\d+) \([^)]+\) shlat-ms (?<XdrDcShlatMs>[0-9\\.]+) errcl (?<XdrDcErrcl>\d+) errsrv (?<XdrDcErrsrv>\d+) rsas-ms (?<XdrDcRsasMs>[0-9\\.]+) rsas-pct (?<XdrDcRsasPct>[0-9\\.]+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 33 
    Name aero_log_parser_type_33
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} from-proxy-batch-sub: tsvc \((?<BtSuFPTsvcErr>\d+),(?<BtSuFPTsvcTout>\d+)\) read \((?<BtSuFPReadOk>\d+),(?<BtSuFPReadErr>\d+),(?<BtSuFPReadTout>\d+),(?<BtSuFPReadNFnd>\d+),(?<BtSuFPReadFltr>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 34 
    Name aero_log_parser_type_34
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+(?<signal>[^ ]+) received, (?<stop>\d+) shutting down$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 35 
    Name aero_log_parser_type_35
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+in-progress: info-q (?<InfoQ>\d+) rw-hash (?<RWHash>\d+) proxy-hash (?<ProxyHash>\d+) tree-gc-q (?<TreeGcQ>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 36 
    Name aero_log_parser_type_36
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} memory-usage: total-bytes (?<NsMemTotalBytes>\d+) index-bytes (?<NsMemIndexBytes>\d+) sindex-bytes (?<NsMemSindexByte>\d+) data-bytes (?<NsMemDataByte>\d+) used-pct (?<NsMemUsedPct>[0-9\\.]+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 37 
    Name aero_log_parser_type_37
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} retransmits: migration (?<ResmitMigra>\d+) all-read (?<ResmitRead>\d+) all-write \((?<ResmitWrtDup>\d+),(?<ResmitWrtRepl>\d+)\) all-delete \((?<ResmitDelDup>\d+),(?<ResmitDelRepl>\d+)\) all-udf \((?<ResmitUdfDup>\d+),(?<ResmitRepl>\d+)\) all-batch-sub (?<ResmitBtchDup>\d+) udf-sub \((?<ResmitUdfDupSub>\d+),(?<ResmitUdfRepl>\d+)\) ops-sub \((?<ResmitOpsDup>\d+),(?<ResmitOpsRepl>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 38 
    Name aero_log_parser_type_38
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+summary: throughput (?<xdrThroughput>\d+) inflight (?<xdrInFlight>\d+) dlog-outstanding (?<xdrDlgOutstand>\d+) dlog-delta-per-sec (?<xdrDlogDeltaSec>[-]*[0-9\\.]+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 39 
    Name aero_log_parser_type_39
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+system-memory: free-kbytes (?<FreeMemKB>\d+) free-pct (?<FreeMemPct>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 40 
    Name aero_log_parser_type_40
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} pmem-usage: used-bytes (?<PmemUseByte>\d+) avail-pct (?<PmemAvailPct>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 41 
    Name aero_log_parser_type_41
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} sindex-gc: Processed: (?<sindexGcProc>\d+), found:(?<sindexGcFound>\d+), deleted: (?<sindexGcDel>\d+): Total time: (?<sindexGcMs>\d+) ms$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 42 
    Name aero_log_parser_type_42
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+histogram dump: (?<Histogram>[^ ]+) \((?<total>\d+) total\) msec *(?<histogram>.*)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 43 
    Name aero_log_parser_type_43
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+histogram dump: {(?<Namespace>[^}]+)}-(?<HistogramCount>[^ ]+) \((?<total>\d+) total\) count *(?<histogram>.*)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 44 
    Name aero_log_parser_type_44
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} tombstones: all (?<TombAll>\d+) master (?<TombMaster>\d+) prole (?<TombProle>\d+) non-replica (?<TombNonRepl>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 45 
    Name aero_log_parser_type_45
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} objects: all (?<ObjectsAll>\d+) master (?<ObjectsMaster>\d+) prole (?<ObjectsProle>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 46 
    Name aero_log_parser_type_46
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+regex:  config-set command $
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 47 
    Name aero_log_parser_type_47
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} nsup-done: evict-threads (?<nsupEvictThrds>\d+) expire-threads (?<nsupExpireThrds>\d+) master-objects \((?<nsupMasterObj>\d+),(?<nsupNonExpirabl>\d+)\) expired \((?<nsupExpireTotal>\d+),(?<nsupExpireNow>\d+)\) evicted \((?<nsupEvictTotal>\d+),(?<nsupEvictNow>\d+)\) evict-ttl (?<nsupEvictTtl>[0-9\\-]+) waits \((?<nsupGeneWaits>\d+),(?<nsupClearWaits>\d+)\) total-ms (?<nsupTotalMs>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 48 
    Name aero_log_parser_type_48
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+(?<restart>\d+) build (?<build>[^ ]+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 49 
    Name aero_log_parser_type_49
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} query: basic \((?<QueryBasicOk>\d+),(?<QueryBasicAbrt>\d+)\) aggr \((?<QueryAggrOk>\d+),(?<QueryAggrAbrt>\d+)\) udf-bg \((?<QueryUdfOk>\d+),(?<QueryUdfErr>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 50 
    Name aero_log_parser_type_50
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+(?<stop>\d+) build (?<build>[^ ]+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 51 
    Name aero_log_parser_type_51
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} tombstones: all (?<TombAll>\d+) xdr \((?<TombXdr>\d+),(?<TombXdrCem>\d+)\) master (?<TombMaster>\d+) prole (?<TombProle>\d+) non-replica (?<TombNonRepl>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 52 
    Name aero_log_parser_type_52
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+CLUSTER-SIZE (?<ClusterSize>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 53 
    Name aero_log_parser_type_53
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} retransmits: migration (?<ResmitMigra>\d+) client-read (?<ResmitRead>\d+) client-write \((?<ResmitWrtDup>\d+),(?<ResmitWrtRepl>\d+)\) client-delete \((?<ResmitDelDup>\d+),(?<ResmitDelRepl>\d+)\) client-udf \((?<ResmitUdfDup>\d+),(?<ResmitRepl>\d+)\) batch-sub (?<ResmitBtchDup>\d+) udf-sub \((?<ResmitUdfDupSub>\d+),(?<ResmitUdfRepl>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 54 
    Name aero_log_parser_type_54
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+histogram dump: (?<HistogramCount>[^ ]+) \((?<total>\d+) total\) count *(?<histogram>.*)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 55 
    Name aero_log_parser_type_55
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} pi-query: short-basic \((?<ScanBasicOk>\d+),(?<ScanBasicErr>\d+),(?<ScanBasicAbrt>\d+)\) long-basic \((?<ScanLongOk>\d+),(?<ScanLongErr>\d+),(?<ScanLongAbrt>\d+)\) aggr \((?<ScanAggrOk>\d+),(?<ScanAggrErr>\d+),(?<ScanAggrAbrt>\d+)\) udf-bg \((?<ScanUdfOk>\d+),(?<ScanUdfErr>\d+),(?<ScanUdfAbrt>\d+)\) ops-bg \((?<ScanOpsOk>\d+),(?<ScanOpsErr>\d+),(?<ScanOpsAbrt>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 56 
    Name aero_log_parser_type_56
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} xdr-tomb-raid-done: dropped (?<xdr5tombDrop>\d+) total-ms (?<xdr5tombMs>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 57 
    Name aero_log_parser_type_57
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} re-repl: tsvc \((?<ReReplTsvcErr>\d+),(?<ReReplTsvcTout>\d+)\) all-triggers \((?<ReReplAllTrgOk>\d+),(?<ReReplAllTrgErr>\d+),(?<ReReplAllTrgTou>\d+)\) unreplicated-records (?<ReReplUn>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 58 
    Name aero_log_parser_type_58
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+system: total-cpu-pct (?<TotalCpuPct>\d+) user-cpu-pct (?<UserCpuPct>\d+) kernel-cpu-pct (?<KernelCpuPct>\d+) free-mem-kbytes (?<FreeMemKB>\d+) free-mem-pct (?<FreeMemPct>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 59 
    Name aero_log_parser_type_59
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+ \((?<errCtx>[^)]+)\): \((?<errC>[^)]+)\) \(repeated:(?<repeated>\d+)\) (?<errMessage>.+$)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 60 
    Name aero_log_parser_type_60
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} appeals: remaining-tx (?<ApplRemainTx>\d+) remaining-rx (?<ApplRemainRx>\d+) active \((?<ApplActTx>\d+),(?<ApplActRx>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 61 
    Name aero_log_parser_type_61
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+(?<signal>[^ ]+) received, shutting down Aerospike Enterprise Edition (?<stop>\d+) build (?<build>.+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 62 
    Name aero_log_parser_type_62
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+(?<stop>\d+) starting normal shutdown$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 63 
    Name aero_log_parser_type_63
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} (?<quiesced>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 64 
    Name aero_log_parser_type_64
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} query: basic \((?<QueryBasicOk>\d+),(?<QueryBasicAbrt>\d+)\) aggr \((?<QueryAggrOk>\d+),(?<QueryAggrAbrt>\d+)\) udf-bg \((?<QueryUdfOk>\d+),(?<QueryUdfErr>\d+)\) ops-bg \((?<QueryOpsOk>\d+),(?<QueryOpsErr>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 65 
    Name aero_log_parser_type_65
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+[A-Z][a-z]{2} [0-9]{2} [0-9]{2}:[0-9]{2}:[0-9]{2}$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 66 
    Name aero_log_parser_type_66
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+ \(norepeat:(?<repeated>\d+)\) \((?<errCtx>[^)]+)\): \((?<errC>[^)]+)\) (?<errMessage>.+$)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 67 
    Name aero_log_parser_type_67
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} appeals: remaining-tx (?<ApplRemainTx>\d+) active \((?<ApplActTx>\d+),(?<ApplActRx>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 68 
    Name aero_log_parser_type_68
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+in-progress: tsvc-q (?<TsvcQ>\d+) info-q (?<InfoQ>\d+) nsup-delete-q (?<NsupDelQ>\d+) rw-hash (?<RWHash>\d+) proxy-hash (?<ProxyHash>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 69 
    Name aero_log_parser_type_69
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} (?<Device>[^:]+): used-bytes (?<UsedBytes>\d+) free-wblocks (?<FreeWBlocks>\d+) write-q (?<WriteQ>\d+) write \((?<Writes>\d+),(?<WritePerSec>[0-9\\.]+)\) defrag-q (?<DefragQ>\d+) defrag-read \((?<DefragR>\d+),(?<DefragRPerSec>[0-9\\.]+)\) defrag-write \((?<DefragW>\d+),(?<DefragWPerSec>[0-9\\.]+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 70 
    Name aero_log_parser_type_70
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+(?<signal>[^ ]+) received, aborting Aerospike Enterprise Edition (?<stop>\d+) build (?<build>.+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 71 
    Name aero_log_parser_type_71
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} appeals: remaining-rx (?<ApplRemainRx>\d+) active \((?<ApplActTx>\d+),(?<ApplActRx>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 72 
    Name aero_log_parser_type_72
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+process: cpu-pct (?<ProcessCpuPct>\d+) heap-kbytes \((?<HeapKBAlloc>\d+),(?<HeapKBActive>\d+),(?<HeapKBMapped>\d+)\) heap-efficiency-pct (?<HeapEfficPct>[0-9\\.]+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 73 
    Name aero_log_parser_type_73
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+device (?<Device>[^:]+): used (?<UsedBytes>\d+), contig-free [^ ]+ \((?<FreeWBlocks>\d+) wblocks\), swb-free (?<SwbFree>\d+), w-q (?<WriteQ>\d+) w-tot (?<Writes>\d+) \((?<WritePerSec>[0-9\\.]+)/s\), defrag-q (?<DefragQ>\d+) defrag-tot (?<DefragR>\d+) \((?<DefragRPerSec>[0-9\\.]+)/s\) defrag-w-tot (?<DefragW>\d+) \((?<DefragWPerSec>[0-9\\.]+)/s\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 74 
    Name aero_log_parser_type_74
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} from-proxy-batch-sub: tsvc \((?<BtSuFPTsvcErr>\d+),(?<BtSuFPTsvcTout>\d+)\) read \((?<BtSuFPReadOk>\d+),(?<BtSuFPReadErr>\d+),(?<BtSuFPReadTout>\d+),(?<BtSuFPReadNFnd>\d+),(?<BtSuFPReadFltr>\d+)\) write \((?<BtSuFPWriteOk>\d+),(?<BtSuFPWriteErr>\d+),(?<BtSuFPWriteTout>\d+),(?<BtSuFPWriteFltr>\d+)\) delete \((?<BtSuFPDelOk>\d+),(?<BtSuFPDelErr>\d+),(?<BtSuFPDelTout>\d+),(?<BtSuFPDelNFnd>\d+),(?<BtSuFPDelFltr>\d+)\) udf \((?<BtSuFPUdfOk>\d+),(?<BtSuFPUdfErr>\d+),(?<BtSuFPUdfTout>\d+),(?<BtSuFPUdfFltr>\d+)\) lang \((?<BtSuFPLangRdOk>\d+),(?<BtSuFPLangWtOk>\d+),(?<BtSuFPLangDlOk>\d+),(?<BtSuFPLangErr>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 75 
    Name aero_log_parser_type_75
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} set-index-usage: used-bytes (?<setidxUsedBytes>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 76 
    Name aero_log_parser_type_76
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} index-usage: used-bytes (?<idxUsedBytes>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 77 
    Name aero_log_parser_type_77
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+in-progress: tsvc-q (?<TsvcQ>\d+) info-q (?<InfoQ>\d+) nsup-delete-q (?<NsupDelQ>\d+) rw-hash (?<RWHash>\d+) proxy-hash (?<ProxyHash>\d+) tree-gc-q (?<TreeGcQ>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 78 
    Name aero_log_parser_type_78
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} batch-sub: tsvc \((?<BtcSubTsvcErr>\d+),(?<BtcSubTsvcTout>\d+)\) proxy \((?<BtcSubPrxyOk>\d+),(?<BtcSubPrxyErr>\d+),(?<BtcSubPrxyTout>\d+)\) read \((?<BtcSubReadOk>\d+),(?<BtcSubReadErr>\d+),(?<BtcSubReadTout>\d+),(?<BtcSubReadNFnd>\d+),(?<BtcSubReadFltr>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 79 
    Name aero_log_parser_type_79
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} retransmits: migration (?<ResmitMigra>\d+) dup-res \((?<ResmitRead>\d+),(?<ResmitWrtDup>\d+),(?<ResmitDelDup>\d+),(?<ResmitUdfDupSub>\d+),(?<ResmitBRDup>\d+),(?<ResmitBWDup>\d+),(?<ResmitBDDup>\d+),(?<ResmitBUDup>\d+),(?<ResmitUdfDupSub>\d+),(?<ResmitOpsDup>\d+)\) repl-ping \((?<ResmitRPRepl>\d+),(?<ResmitBPRepl>\d+)\) repl-write \((?<ResmitWrtRepl>\d+),(?<ResmitDelRepl>\d+),(?<ResmitRepl>\d+),(?<ResmitBWRepl>\d+),(?<ResmitBDRepl>\d+),(?<ResmitBURepl>\d+),(?<ResmitUdfRepl>\d+),(?<ResmitOpsRepl>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 80 
    Name aero_log_parser_type_80
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} sindex-gc-done: cleaned \((?<sindexGcClnA>\d+),(?<sindexGcClnC>\d+)\) total-ms (?<sindexGcMs>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 81 
    Name aero_log_parser_type_81
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+batch-index: batches \((?<BatchSuccess>\d+),(?<BatchError>\d+),(?<BatchTimeout>\d+)\) delays (?<BatchDelay>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 82 
    Name aero_log_parser_type_82
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} sindex-gc-done: processed (?<sindexGcProc>\d+) found (?<sindexGcFound>\d+) deleted (?<sindexGcDel>\d+) total-ms (?<sindexGcMs>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 83 
    Name aero_log_parser_type_83
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} scan: basic \((?<ScanBasicOk>\d+),(?<ScanBasicErr>\d+),(?<ScanBasicAbrt>\d+)\) aggr \((?<ScanAggrOk>\d+),(?<ScanAggrErr>\d+),(?<ScanAggrAbrt>\d+)\) udf-bg \((?<ScanUdfOk>\d+),(?<ScanUdfErr>\d+),(?<ScanUdfAbrt>\d+)\) ops-bg \((?<ScanOpsOk>\d+),(?<ScanOpsErr>\d+),(?<ScanOpsAbrt>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 84 
    Name aero_log_parser_type_84
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+config-set (?<configset>\d+) command completed: params (?<command>.*)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 85 
    Name aero_log_parser_type_85
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+(?<stopDone>\d+) finished clean shutdown - exiting$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 86 
    Name aero_log_parser_type_86
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+(?<signal>[^ ]+) received, (?<stop>\d+) starting normal shutdown$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 87 
    Name aero_log_parser_type_87
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} xdr-from-proxy: write \((?<XdrWrFrPrxOk>\d+),(?<XdrWrFrPrxErr>\d+),(?<XdrWrFrPrxTout>\d+)\) delete \((?<XdrDelFrPrxOk>\d+),(?<XdrDelFrPrxErr>\d+),(?<XdrDelFrPrxTout>\d+),(?<XdrDelFrPrxNFou>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 88 
    Name aero_log_parser_type_88
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+early-fail: demarshal (?<EarlyFailDemar>\d+) tsvc-client (?<EarlyFailTsvc>\d+) tsvc-batch-sub (?<EarlyFailBchSub>\d+) tsvc-udf-sub (?<EarlyFailUdfSub>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 89 
    Name aero_log_parser_type_89
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+[A-Z][a-z]{2} [0-9]{2} [0-9]{4} [0-9]{2}:[0-9]{2}:[0-9]{2}$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 90 
    Name aero_log_parser_type_90
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+xdr-dc (?<xdr5dc>[^:]+): nodes (?<xdr5nodes>\d+) lag (?<xdr5lag>\d+) throughput (?<xdr5throughput>\d+) latency-ms (?<xdr5latencyms>\d+) in-queue (?<xdr5inqueue>\d+) in-progress (?<xdr5outstanding>\d+) complete \((?<xdr5DoneOk>\d+),(?<xdr5DoneAbandon>\d+),(?<xdr5DoneNotFnd>\d+),(?<xdr5DoneFOut>\d+)\) retries \((?<xdr5RetrNoNode>\d+),(?<xdr5RetrConnRst>\d+),(?<xdr5RetrDest>\d+)\) recoveries \((?<xdr5Recov>\d+),(?<xdr5RecovPend>\d+)\) hot-keys (?<xdr5HotKeys>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 91 
    Name aero_log_parser_type_91
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} dup-res: ask (?<dupresAsk>\d+) respond \((?<dupresRespRd>\d+),(?<dupresRespNo>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 92 
    Name aero_log_parser_type_92
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+ \((?<warnCtx>[^)]+)\): \((?<warnC>[^)]+)\) \(repeated:(?<repeated>\d+)\) (?<warnMessage>.+$)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 93 
    Name aero_log_parser_type_93
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} index-pmem-usage: used-bytes (?<IdxPmemUseByte>\d+) used-pct (?<IdxPmemUsePct>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 94 
    Name aero_log_parser_type_94
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} migrations: remaining \((?<MigraRemainTx>\d+),(?<MigraRemainRx>\d+),(?<MigraRemainSig>\d+)\) active \((?<MigraActiveTx>\d+),(?<MigraActiveRx>\d+),(?<MigraActiveSig>\d+)\) complete-pct (?<MigraCompletPct>[0-9\\.]+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 95 
    Name aero_log_parser_type_95
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+heartbeat-received: self (?<HBSelf>\d+) foreign (?<HBForeign>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 96 
    Name aero_log_parser_type_96
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+histogram dump: {(?<Namespace>[^}]+)}-(?<HistogramUs>[^ ]+) \((?<total>\d+) total\) usec *(?<histogram>.*)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 97 
    Name aero_log_parser_type_97
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} si-query: short-basic \((?<QueryBasicOk>\d+),(?<QueryBasicErr>\d+),(?<QueryBasicAbrt>\d+)\) long-basic \((?<QueryLongOk>\d+),(?<QueryLongErr>\d+),(?<QueryLongAbrt>\d+)\) aggr \((?<QueryAggrOk>\d+),(?<QueryAggrErr>\d+),(?<QueryAggrAbrt>\d+)\) udf-bg \((?<QueryUdfOk>\d+),(?<QueryUdfErr>\d+),(?<QueryUdfAbrt>\d+)\) ops-bg \((?<QueryOpsOk>\d+),(?<QueryOpsErr>\d+),(?<QueryOpsAbrt>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 98 
    Name aero_log_parser_type_98
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+sub:  quiesced this 1 node$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 99 
    Name aero_log_parser_type_99
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} batch-sub: tsvc \((?<BtcSubTsvcErr>\d+),(?<BtcSubTsvcTout>\d+)\) proxy \((?<BtcSubPrxyOk>\d+),(?<BtcSubPrxyErr>\d+),(?<BtcSubPrxyTout>\d+)\) read \((?<BtcSubReadOk>\d+),(?<BtcSubReadErr>\d+),(?<BtcSubReadTout>\d+),(?<BtcSubReadNFnd>\d+),(?<BtcSubReadFltr>\d+)\) write \((?<BtcSubWriteOk>\d+),(?<BtcSubWriteErr>\d+),(?<BtcSubWriteTout>\d+),(?<BtcSubWriteFltr>\d+)\) delete \((?<BtcSubDelOk>\d+),(?<BtcSubDelErr>\d+),(?<BtcSubDelTout>\d+),(?<BtcSubDelNFnd>\d+),(?<BtcSubDelFltr>\d+)\) udf \((?<BtcSubUdfOk>\d+),(?<BtcSubUdfErr>\d+),(?<BtcSubUdfTout>\d+),(?<BtcSubUdfFltr>\d+)\) lang \((?<BtcSubLangRdOk>\d+),(?<BtcSubLangWtOk>\d+),(?<BtcSubLangDlOk>\d+),(?<BtcSubLangErr>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 100 
    Name aero_log_parser_type_100
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} ops-sub: tsvc \((?<OpsSubTsvcErr>\d+),(?<OpsSubTsvcTout>\d+)\) write \((?<OpsSubWrOk>\d+),(?<OpsSubWrErr>\d+),(?<OpsSubWrTout>\d+),(?<OpsSubWrFOut>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 101 
    Name aero_log_parser_type_101
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} breached stop-writes limit \((?<stopWrReason>[^)]+)\), sys-memory pct:(?<stopWrSysMemPct>\d+), indexes-memory sz:(?<stopWrIdxMemSz>\d+) \((?<stopWrIdx>\d+) + (?<stopWrSetIdx>\d+) + (?<stopWrSetSIdx>\d+)\), data avail-pct:(?<stopWrDatAvail>\d+) used-pct:(?<stopWrUsed>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 102 
    Name aero_log_parser_type_102
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+regex:  un-quiesced this node$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 103 
    Name aero_log_parser_type_103
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+histogram dump: {(?<Namespace>[^}]+)}-(?<Histogram>[^ ]+) \((?<total>\d+) total\) msec *(?<histogram>.*)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 104 
    Name aero_log_parser_type_104
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+regex:  build $
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 105 
    Name aero_log_parser_type_105
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+ \(norepeat:(?<repeated>\d+)\) \((?<warnCtx>[^)]+)\): \((?<warnC>[^)]+)\) (?<warnMessage>.+$)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 106 
    Name aero_log_parser_type_106
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+\\[(?<XdrDcName>[^]]+)\\]: dc-state (?<XdrDcState>[^ ]+) timelag-sec (?<XdrDcTimelag>\d+) lst (?<XdrDcLst>\d+) mlst (?<XdrDcMlst>\d+) \([^)]+\) fnlst (?<XdrDcFnlst>\d+) \([^)]+\) wslst (?<XdrDcWslst>\d+) \([^)]+\) shlat-ms (?<XdrDcShlatMs>[0-9\\.]+) rsas-ms (?<XdrDcRsasMs>[0-9\\.]+) rsas-pct (?<XdrDcRsasPct>[0-9\\.]+) con (?<XdrDcOpenConn>\d+) errcl (?<XdrDcErrcl>\d+) errsrv (?<XdrDcErrsrv>\d+) sz (?<XdrDcSz>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 107 
    Name aero_log_parser_type_107
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} retransmits: migration (?<ResmitMigra>\d+) all-read (?<ResmitRead>\d+) all-write \((?<ResmitWrtDup>\d+),(?<ResmitWrtRepl>\d+)\) all-delete \((?<ResmitDelDup>\d+),(?<ResmitDelRepl>\d+)\) all-udf \((?<ResmitUdfDup>\d+),(?<ResmitRepl>\d+)\) all-batch-sub (?<ResmitBtchDup>\d+) udf-sub \((?<ResmitUdfDupSub>\d+),(?<ResmitUdfRepl>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 108 
    Name aero_log_parser_type_108
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} sindex-usage: used-bytes (?<sidxUsedBytes>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 109 
    Name aero_log_parser_type_109
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+early-fail: demarshal (?<EarlyFailDemar>\d+) tsvc-client (?<EarlyFailTsvc>\d+) tsvc-from-proxy (?<EarlyFailFrmPrx>\d+) tsvc-batch-sub (?<EarlyFailBchSub>\d+) tsvc-from-proxy-batch-sub (?<EarlyFFPBchSub>\d+) tsvc-udf-sub (?<EarlyFailUdfSub>\d+) tsvc-ops-sub (?<EarlyFailOpsSub>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 110 
    Name aero_log_parser_type_110
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+sub:  config-set 1 command $
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 111 
    Name aero_log_parser_type_111
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+histogram dump: (?<HistogramUs>[^ ]+) \((?<total>\d+) total\) usec *(?<histogram>.*)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 112 
    Name aero_log_parser_type_112
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} nsup-start: evict-threads (?<nsupEvictThrds>\d+) evict-ttl (?<nsupEvictTtl>\d+) evict-void-time \((?<nsupEvictVoidEf>\d+),(?<nsupEvictVoidSp>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 113 
    Name aero_log_parser_type_113
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} tombstones: all (?<TombAll>\d+) xdr (?<TombXdr>\d+) master (?<TombMaster>\d+) prole (?<TombProle>\d+) non-replica (?<TombNonRepl>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 114 
    Name aero_log_parser_type_114
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+xdr-dc (?<xdr5dc>[^:]+): nodes (?<xdr5nodes>\d+) latency-ms (?<xdr5latencyms>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 115 
    Name aero_log_parser_type_115
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+skew-ms (?<ClockSkewMs>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 116 
    Name aero_log_parser_type_116
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} scan: basic \((?<ScanBasicOk>\d+),(?<ScanBasicErr>\d+),(?<ScanBasicAbrt>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 117 
    Name aero_log_parser_type_117
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+detail: sh (?<xdrSh>\d+) ul (?<xdrUl>\d+) lg (?<xdrLg>\d+) rlg (?<xdrRlg>\d+) rlgi (?<xdrRlgi>\d+) rlgo (?<xdrRlgo>\d+) lproc (?<xdrLproc>\d+) rproc (?<xdrRproc>\d+) lkdproc (?<xdrLkdproc>\d+) errcl (?<xdrErrcl>\d+) errsrv (?<xdrErrSrv>\d+) hkskip (?<xdrHkskip>\d+) hkf (?<xdrHkf>\d+) flat (?<xdrFlat>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 118 
    Name aero_log_parser_type_118
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+histogram dump: {(?<Namespace>[^}]+)}-(?<HistogramSize>[^ ]+) \((?<total>\d+) total\) bytes *(?<histogram>.*)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 119 
    Name aero_log_parser_type_119
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} objects: all (?<ObjectsAll>\d+) master (?<ObjectsMaster>\d+) prole (?<ObjectsProle>\d+) non-replica (?<ObjectsNonProle>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 120 
    Name aero_log_parser_type_120
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+process: cpu-pct (?<ProcessCpuPct>\d+) threads \((?<ThreadJoin>\d+),(?<ThreadDetach>\d+),(?<ThreadPoolTot>\d+),(?<ThreadPoolAct>\d+)\) heap-kbytes \((?<HeapKBAlloc>\d+),(?<HeapKBActive>\d+),(?<HeapKBMapped>\d+)\) heap-efficiency-pct (?<HeapEfficPct>[0-9\\.]+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 121 
    Name aero_log_parser_type_121
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} re-repl: all-triggers \((?<ReReplAllTrgOk>\d+),(?<ReReplAllTrgErr>\d+),(?<ReReplAllTrgTou>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 122 
    Name aero_log_parser_type_122
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} from-proxy-batch-sub: tsvc \((?<BtSuFPTsvcErr>\d+),(?<BtSuFPTsvcTout>\d+)\) read \((?<BtSuFPReadOk>\d+),(?<BtSuFPReadErr>\d+),(?<BtSuFPReadTout>\d+),(?<BtSuFPReadNFnd>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 123 
    Name aero_log_parser_type_123
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} udf-sub: tsvc \((?<UdfSubTsvcErr>\d+),(?<UdfSubTsvcTout>\d+)\) udf \((?<UdfSubUdfOk>\d+),(?<UdfSubUdfErr>\d+),(?<UdfSubUdfTout>\d+),(?<UdfSubUdfFOut>\d+)\) lang \((?<UdfSubLangRdOk>\d+),(?<UdfSubLangWrOk>\d+),(?<UdfSubLangDelOk>\d+),(?<UdfSubLangErr>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 124 
    Name aero_log_parser_type_124
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} rebalanced: regime (?<reRegime>\d+) expected-migrations \((?<reTx>\d+),(?<reRx>\d+),(?<reSig>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 125 
    Name aero_log_parser_type_125
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+system: total-cpu-pct (?<TotalCpuPct>\d+) user-cpu-pct (?<UserCpuPct>\d+) kernel-cpu-pct (?<KernelCpuPct>\d+) free-mem-kbytes (?<FreeMemKB>\d+) free-mem-pct (?<FreeMemPct>\d+) thp-mem-kbytes (?<THPMemKB>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 126 
    Name aero_log_parser_type_126
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+histogram dump: {(?<HistogramSize>[^ ]+) \((?<total>\d+) total\) bytes *(?<histogram>.*)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 127 
    Name aero_log_parser_type_127
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+sub:  un-quiesced this 0 node$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 128 
    Name aero_log_parser_type_128
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+(?<stop>\d+) shutting down$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 129 
    Name aero_log_parser_type_129
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} nsup-start: evict-ttl (?<nsupEvictTtl>\d+) evict-void-time \((?<nsupEvictVoidEf>\d+),(?<nsupEvictVoidSp>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 130 
    Name aero_log_parser_type_130
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+allowing (?<MigraFillCt>\d+) fill-migrations after (?<MigraFillSec>\d+) seconds delay$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 131 
    Name aero_log_parser_type_131
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+xdr-dc (?<xdr5dc>[^:]+): lag (?<xdr5lag>\d+) throughput (?<xdr5throughput>\d+) latency-ms (?<xdr5latencyms>\d+) in-queue (?<xdr5inqueue>\d+) in-progress (?<xdr5outstanding>\d+) complete \((?<xdr5DoneOk>\d+),(?<xdr5DoneAbandon>\d+),(?<xdr5DoneNotFnd>\d+),(?<xdr5DoneFOut>\d+)\) retries \((?<xdr5RetrNoNode>\d+),(?<xdr5RetrConnRst>\d+),(?<xdr5RetrDest>\d+)\) recoveries \((?<xdr5Recov>\d+),(?<xdr5RecovPend>\d+)\) hot-keys (?<xdr5HotKeys>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 132 
    Name aero_log_parser_type_132
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} breached eviction limit \((?<evictReason>[^)]+)\), sys-memory pct:(?<evictSysMemPct>\d+), indexes-memory sz:(?<evictIdxMemSz>\d+) \((?<evictIdx>\d+) + (?<evictSetIdx>\d+) + (?<evictSetSIdx>\d+)\), index-device sz:(?<evictIdxDev>\d+) used-pct (?<evictUsedPct>\d+), data used-pct:(?<evictDataUsed>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 133 
    Name aero_log_parser_type_133
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} udf-sub: tsvc \((?<UdfSubTsvcErr>\d+),(?<UdfSubTsvcTout>\d+)\) udf \((?<UdfSubUdfOk>\d+),(?<UdfSubUdfErr>\d+),(?<UdfSubUdfOkTout>\d+)\) lang \((?<UdfSubLangRdOk>\d+),(?<UdfSubLangWrOk>\d+),(?<UdfSubLangDelOk>\d+),(?<UdfSubLangErr>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 134 
    Name aero_log_parser_type_134
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} from-proxy: tsvc \((?<FrPrxyTsvcErr>\d+),(?<FrPrxyTsvcTout>\d+)\) read \((?<FrPrxyReadOk>\d+),(?<FrPrxyReadErr>\d+),(?<FrPrxyReadTout>\d+),(?<FrPrxyReadNFnd>\d+),(?<FrPrxyReadFltr>\d+)\) write \((?<FrPrxyWrtOk>\d+),(?<FrPrxyWrtErr>\d+),(?<FrPrxyWrtTout>\d+),(?<FrPrxyWrtFltr>\d+)\) delete \((?<FrPrxyDelOk>\d+),(?<FrPrxyDelErr>\d+),(?<FrPrxyDelTout>\d+),(?<FrPrxyDelNFnd>\d+),(?<FrPrxyDelFltr>\d+)\) udf \((?<FrPrxyUdfOk>\d+),(?<FrPrxyUdfErr>\d+),(?<FrPrxyUdfTout>\d+),(?<FrPrxyUdfFltr>\d+)\) lang \((?<FrPrxyLangRead>\d+),(?<FrPrxyLangWrite>\d+),(?<FrPrxyLangDel>\d+),(?<FrPrxylangErr>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 135 
    Name aero_log_parser_type_135
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+xdr-dc (?<xdr5dc>[^:]+): lag (?<xdr5lag>\d+) throughput (?<xdr5throughput>\d+) latency-ms (?<xdr5latencyms>\d+) in-queue (?<xdr5inqueue>\d+) in-progress (?<xdr5outstanding>\d+) complete \((?<xdr5DoneOk>\d+),(?<xdr5DoneAbandon>\d+),(?<xdr5DoneNotFnd>\d+),(?<xdr5DoneFOut>\d+)\) retries \((?<xdr5RetrConnRst>\d+),(?<xdr5RetrDest>\d+)\) recoveries \((?<xdr5Recov>\d+),(?<xdr5RecovPend>\d+)\) hot-keys (?<xdr5HotKeys>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 136 
    Name aero_log_parser_type_136
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} Records: (?<nsupMasterObj>\d+), (?<nsupNonExpirabl>\d+) 0-vt, (?<nsupExpireTotal>\d+)\((?<nsupExpireNow>\d+)\) expired, (?<nsupEvictTotal>\d+)\((?<nsupEvictNow>\d+)\) evicted, (?<nsupSetDelTotal>\d+)\((?<nsupSetDelNow>\d+)\) set deletes. Evict ttl: (?<nsupEvictTtl>[0-9\\-]+). Waits: (?<nsupSetWaits>\d+),(?<nsupClearWaits>\d+),(?<nsupGeneWaits>\d+). Total time: (?<nsupTotalMs>\d+) ms$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 137 
    Name aero_log_parser_type_137
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+in-progress: tsvc-q (?<TsvcQ>\d+) info-q (?<InfoQ>\d+) rw-hash (?<RWHash>\d+) proxy-hash (?<ProxyHash>\d+) tree-gc-q (?<TreeGcQ>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 138 
    Name aero_log_parser_type_138
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} (?<Device>[^:]+): used-bytes (?<UsedBytes>\d+) free-wblocks (?<FreeWBlocks>\d+) write \((?<Writes>\d+),(?<WritePerSec>[0-9\\.]+)\) defrag-q (?<DefragQ>\d+) defrag-read \((?<DefragR>\d+),(?<DefragRPerSec>[0-9\\.]+)\) defrag-write \((?<DefragW>\d+),(?<DefragWPerSec>[0-9\\.]+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 139 
    Name aero_log_parser_type_139
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} migrations: remaining \((?<MigraRemainTx>\d+),(?<MigraRemainRx>\d+)\) active \((?<MigraActiveTx>\d+),(?<MigraActiveRx>\d+)\) complete-pct (?<MigraCompletPct>[0-9\\.]+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 140 
    Name aero_log_parser_type_140
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+fabric-bytes-per-second: bulk \((?<FabricBulkTx>\d+),(?<FabricBulkRx>\d+)\) ctrl \((?<FabricCtrlTx>\d+),(?<FabricCtrlRx>\d+)\) meta \((?<FabricMetaTx>\d+),(?<FabricMetaRx>\d+)\) rw \((?<FabricRwTx>\d+),(?<FabricRwRx>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 141 
    Name aero_log_parser_type_141
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} client: tsvc \((?<ClientTsvcErr>\d+),(?<ClientTsvcTout>\d+)\) proxy \((?<ClientPrxyOk>\d+),(?<ClientPrxyErr>\d+),(?<ClientPrxyTout>\d+)\) read \((?<ClientReadOk>\d+),(?<ClientReadErr>\d+),(?<ClientReadTout>\d+),(?<ClientReadNFnd>\d+)\) write \((?<ClientWrtOk>\d+),(?<ClientWrtErr>\d+),(?<ClientWrtTout>\d+)\) delete \((?<ClientDelOk>\d+),(?<ClientDelErr>\d+),(?<ClientDelTout>\d+),(?<ClientDelNFnd>\d+)\) udf \((?<ClientUdfOk>\d+),(?<ClientUdfErr>\d+),(?<ClientUdfTout>\d+)\) lang \((?<ClientLangRead>\d+),(?<ClientLangWrite>\d+),(?<ClientLangDel>\d+),(?<ClientlangErr>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 142 
    Name aero_log_parser_type_142
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} batch-sub: tsvc \((?<BtcSubTsvcErr>\d+),(?<BtcSubTsvcTout>\d+)\) proxy \((?<BtcSubPrxyOk>\d+),(?<BtcSubPrxyErr>\d+),(?<BtcSubPrxyTout>\d+)\) read \((?<BtcSubReadOk>\d+),(?<BtcSubReadErr>\d+),(?<BtcSubReadTout>\d+),(?<BtcSubReadNFnd>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 143 
    Name aero_log_parser_type_143
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} device-usage: used-bytes (?<DevUsedBytes>\d+) avail-pct (?<DevAvailPct>[0-9\\.]+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 144 
    Name aero_log_parser_type_144
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} special-errors: key-busy (?<ErrKeyBusy>\d+) record-too-big (?<ErrRecTooBig>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 145 
    Name aero_log_parser_type_145
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} xdr-client: write \((?<XdrClWrOk>\d+),(?<XdrClWrErr>\d+),(?<XdrClWrTout>\d+)\) delete \((?<XdrClDelOk>\d+),(?<XdrClDelErr>\d+),(?<XdrClDelTout>\d+),(?<XdrClDelNFou>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 146 
    Name aero_log_parser_type_146
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} re-repl: tsvc \((?<ReReplTsvcErr>\d+),(?<ReReplTsvcTout>\d+)\) all-triggers \((?<ReReplAllTrgOk>\d+),(?<ReReplAllTrgErr>\d+),(?<ReReplAllTrgTou>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 147 
    Name aero_log_parser_type_147
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+early-fail: demarshal (?<EarlyFailDemar>\d+) tsvc-client (?<EarlyFailTsvc>\d+) tsvc-from-proxy (?<EarlyFailFrmPrx>\d+) tsvc-batch-sub (?<EarlyFailBchSub>\d+) tsvc-from-proxy-batch-sub (?<EarlyFFPBchSub>\d+) tsvc-udf-sub (?<EarlyFailUdfSub>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 148 
    Name aero_log_parser_type_148
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} data-usage: used-bytes (?<dataUsedBytes>\d+) avail-pct (?<dataAvailPct>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 149 
    Name aero_log_parser_type_149
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} rebalanced: expected-migrations \((?<reTx>\d+),(?<reRx>\d+),(?<reSig>\d+)\)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 150 
    Name aero_log_parser_type_150
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} rebalanced: regime (?<reRegime>\d+) expected-migrations \((?<reTx>\d+),(?<reRx>\d+),(?<reSig>\d+)\) expected-appeals (?<reAppeals>\d+) dead-partitions (?<reDead>\d+)$
    Time_Key    datetime

[PARSER]
    # Aerospike RegEx patterns - 151 
    Name aero_log_parser_type_151
    Format regex
    Regex   ^(?<datetimestamp>\w+ \d+ \d+ \d+:\d+:\d+) (?<timezone>\S+): (?<log_level>\S+) \((?<source>\S+)\): \((?<source_location>[^)]+)\)\s+{(?<Namespace>[^}]+)} sindex-pmem-usage: used-bytes (?<SdxPmemUseByte>\d+) used-pct (?<SdxPmemUsePct>\d+)$
    Time_Key    datetime

